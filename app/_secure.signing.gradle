android {
    signingConfigs {
        release {
            storeFile rootProject.file("atlasappwidget-release.jks")
            storePassword ""
            keyAlias ""
            keyPassword ""
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
        }
    }
}
